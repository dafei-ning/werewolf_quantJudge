import 'package:flutter/material.dart';

class GameIntroduction extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(30),
      child: Text("GameIntroduction"),
      color: Colors.teal[300],
    );
  }
}