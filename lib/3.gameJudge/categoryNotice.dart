import 'package:flutter/material.dart';

class CategoryNotice extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(20),
      child: Text("Category Notice"),
      color: Colors.grey[100],
    );
  }
}
