import 'package:flutter/material.dart';
import '../../models/gameIntroModel.dart';

class ChoiceConfigChip extends StatelessWidget {
  final MapEntry<int, CharacterSet> charConfig;

  ChoiceConfigChip(this.charConfig);
  @override
  Widget build(BuildContext context) {
    var chip = Container();
    
    return chip;
  }
}
